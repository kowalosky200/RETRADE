name: Stage 2 disposal resurrection audit

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/stage2-lifecycle-date-audit.yml'
      - '.github/scripts/stage2_lifecycle_date_audit.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  extract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Extract disposal persistence and Stock filtering
        run: python .github/scripts/stage2_lifecycle_date_audit.py
      - name: Commit audit report
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add stage2-disposal-resurrection-audit.txt
          if git diff --cached --quiet; then
            echo "No audit report changes"
          else
            git commit -m "chore(audit): capture disposal resurrection paths [skip ci]"
            git push
          fi

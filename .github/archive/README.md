# Archived GitHub tooling

These scripts and workflow definitions record completed one-off repair and audit work. Files below this directory are not active GitHub Actions workflows.

Only current automation belongs in `.github/workflows/`.
